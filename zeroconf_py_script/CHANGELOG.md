# Zeroconf Py Script HA App Changelog

## 0.0.2

  * Change `addon_config` config map to `app_config`
  * Create the configured file automatically, if it does not exist at startup


## 0.0.1

  * Init
